const {formatearPrecio, calcularDescuento} = require("./utilidades.js").default;
const Usuario = require("./usuario.js");
let items = [];
class Carrito extends Usuario{
    constructor(nombre, email, esVIP) {
        
    }
}